---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: external-dns
  namespace: flux-system
spec:
  interval: 1h
  url: oci://ghcr.io/kubernetes-sigs/external-dns
  ref:
    semver: "9.0.*"
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy